{
  "name": "electron-http3",
  "version": "1.0.0",
  "description": "Electron app with HTTP/3 request",
  "main": "main.js",
  "scripts": {
    "start": "electron . --enable-logging --log-file=./metrics.log --no-sandbox --vmodule=*/metrics/*=2"
  },
  "devDependencies": {
    "electron": "^39.2.7"
  }
}
